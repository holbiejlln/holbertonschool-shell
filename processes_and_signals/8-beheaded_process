#!/usr/bin/env bash
# Script that kills the 7-highlander process

# Get the PID of 7-highlander
pid=$(pgrep -f 7-highlander)

# Check if the process exists and kill it
if [ -n "$pid" ]; then
    kill "$pid"
fi

