# 3. Until Best School Loop

This Bash script displays **"Best School"** 10 times using an `until` loop.  
It demonstrates how to use the `until` loop in Bash scripting.

## Usage

1. Make the script executable:

```bash
chmod +x 3-until_best_school

